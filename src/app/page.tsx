import Link from 'next/link';import {Play,Flame,ChevronRight} from 'lucide-react';import {Header} from '@/components/Header';import {VideoCard,type FeedVideo} from '@/components/VideoCard';import {createClient} from '@/lib/supabase/server';
export const dynamic='force-dynamic';
function ago(value:string|null){if(!value)return 'just now';const s=Math.max(0,Math.floor((Date.now()-new Date(value).getTime())/1000));if(s<3600)return `${Math.max(1,Math.floor(s/60))}m ago`;if(s<86400)return `${Math.floor(s/3600)}h ago`;return `${Math.floor(s/86400)}d ago`}
function duration(n:number|null){if(!n)return '';return `${Math.floor(n/60)}:${String(n%60).padStart(2,'0')}`}
function views(n:number){return n>=1e6?`${(n/1e6).toFixed(1)}M`:n>=1e3?`${Math.round(n/1e3)}K`:String(n)}
const Section=({title,id,items}:{title:string;id:string;items:FeedVideo[]})=>items.length?<section className="feed-section" id={id}><div className="section-head"><h2>{title}</h2><span>VIEW ALL <ChevronRight/></span></div><div className="video-grid">{items.map(v=><VideoCard key={v.id} video={v}/>)}</div></section>:null;
export default async function Home(){const supabase=await createClient();const {data:rows}=await supabase.from('videos').select('id,title,slug,thumbnail_path,duration_seconds,is_featured,published_at,category:categories(name),video_events(id)').eq('status','published').order('published_at',{ascending:false}).limit(40);const items:FeedVideo[]=(rows??[]).map((v)=>({id:v.id,title:v.title,slug:v.slug,category:v.category?.[0]?.name??'Culture',views:views(v.video_events?.length??0),age:ago(v.published_at),duration:duration(v.duration_seconds),thumbnailUrl:v.thumbnail_path?supabase.storage.from('thumbnails').getPublicUrl(v.thumbnail_path).data.publicUrl:null}));if(!items.length)return <><Header/><main className="empty-home"><div><span className="feature-pill">TRAPFEED</span><h1>The feed is getting loaded.</h1><p>Published videos will appear here automatically.</p></div></main></>;const featuredId=(rows??[]).find((v)=>v.is_featured)?.id;const hero=items.find(v=>v.id===featuredId)??items[0];const trending=[...items].sort((a,b)=>Number(b.views.replace(/[^0-9.]/g,''))-Number(a.views.replace(/[^0-9.]/g,''))).filter(v=>v.id!==hero.id).slice(0,4);const music=items.filter(v=>v.category.toLowerCase().includes('music')).slice(0,8);const interviews=items.filter(v=>v.category.toLowerCase().includes('interview')).slice(0,8);return <><Header/><main><section className="hero-wrap"><div className="hero"><Link href={`/watch/${hero.slug}`} className="hero-art" style={hero.thumbnailUrl?{backgroundImage:`linear-gradient(0deg,rgba(0,0,0,.75),rgba(0,0,0,.05)),url(${hero.thumbnailUrl})`}:undefined}><div className="hero-glow"/><span className="hero-play"><Play fill="currentColor"/></span><div className="hero-content"><span className="feature-pill">FEATURED</span><h1>{hero.title}</h1><p>{hero.views} views <b>•</b> {hero.age}</p><span className="hero-deck">Watch what the culture is talking about right now.</span></div></Link><aside className="trending"><h2><Flame/> TRENDING NOW</h2>{trending.map((v,i)=><div className="trend-row" key={v.id}><span className="rank">0{i+1}</span><VideoCard video={v} compact/></div>)}</aside></div></section><div className="content-shell"><Section title="Newest Videos" id="videos" items={items.slice(0,8)}/><Section title="Music Videos" id="music" items={music}/><Section title="Interviews" id="interviews" items={interviews}/></div></main><footer><div className="brand">TRAP<span>FEED</span></div><p>HIP-HOP. CULTURE. UNFILTERED.</p><small>© 2026 TrapFeed. All rights reserved.</small></footer></>}
