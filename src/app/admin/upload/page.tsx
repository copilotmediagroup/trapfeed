import Link from 'next/link';import {createUploadDraft} from './actions';
export default async function UploadPage({searchParams}:{searchParams:Promise<{error?:string}>}){const {error}=await searchParams;return <main className="admin-sub"><div className="admin-top"><div><p>NEW CONTENT</p><h1>Upload Video</h1></div><Link className="secondary" href="/admin/videos">VIDEO LIBRARY</Link></div><form action={createUploadDraft} className="cms-form"><h2>Create upload draft</h2><p>Register an authorized video stored in TrapFeed private video bucket. Direct file transfer is the next pipeline gate.</p>{error&&<div className="login-error">{error}</div>}<label>Video title<input name="title" required placeholder="Video title"/></label><label>Storage path<input name="videoPath" required placeholder="uploads/video.mp4"/></label><button className="primary">CREATE DRAFT</button></form></main>}
