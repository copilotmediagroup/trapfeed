import Link from 'next/link'; import {redirect} from 'next/navigation'; import {createClient} from '@/lib/supabase/server'; import {LayoutDashboard,Video,Upload,Link2,Clock3,Tags,Users,MessageSquare,BarChart3,Megaphone,Settings,Plus,Eye} from 'lucide-react'; import {videos} from '@/data/videos';
const nav=[[LayoutDashboard,'Dashboard'],[Video,'Videos'],[Upload,'Upload Video'],[Link2,'YouTube Import'],[Clock3,'Drafts & Scheduled'],[Tags,'Categories'],[Users,'Artists & Users'],[MessageSquare,'Comments'],[BarChart3,'Analytics'],[Megaphone,'Ads'],[Settings,'Settings']] as const;
export default async function Admin(){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user) redirect('/admin/login');const {data:profile}=await supabase.from('profiles').select('role').eq('id',user.id).single();if(!profile||!['editor','admin'].includes(profile.role)) redirect('/');return <div className="admin-shell"><aside className="admin-side"><Link href="/" className="brand">TRAP<span>FEED</span></Link><small>ADMIN CONTROL</small><div className="admin-nav">{nav.map(([Icon,label],i)=><button className={i===0?'active':''} key={label}><Icon/>{label}</button>)}</div><Link href="/" className="back-site">← View public site</Link></aside><main className="admin-main"><div className="admin-top"><div><p>ADMINISTRATION</p><h1>Dashboard</h1></div><button className="primary"><Plus/> ADD VIDEO</button></div><div className="stats"><div><span>TOTAL VIDEOS</span><strong>1,284</strong><em>+24 this week</em></div><div><span>TOTAL VIEWS</span><strong>48.7M</strong><em>+12.4%</em></div><div><span>PUBLISHED</span><strong>1,196</strong><em>93% of library</em></div><div><span>SCHEDULED</span><strong>18</strong><em>Next: 7:00 PM</em></div></div><section className="admin-panel"><div className="panel-head"><div><h2>Recent Videos</h2><p>Manage uploads, YouTube imports and publishing.</p></div><button>VIEW ALL</button></div><div className="admin-table"><div className="table-row table-labels"><span>VIDEO</span><span>SOURCE</span><span>VIEWS</span><span>STATUS</span><span></span></div>{videos.slice(0,6).map((v,i)=><div className="table-row" key={v.id}><div className="table-video"><div className={`mini-thumb ${v.accent}`}/><div><b>{v.title}</b><small>{v.category} • {v.age}</small></div></div><span><i className={i%2?'youtube':'upload'}>{i%2?'YOUTUBE':'UPLOAD'}</i></span><span>{v.views}</span><span><i className="published">PUBLISHED</i></span><button className="icon-btn"><Eye/></button></div>)}</div></section></main></div>}
