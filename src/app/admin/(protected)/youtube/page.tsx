import Link from 'next/link';import {importYoutubeDraft} from './actions';
export default async function YoutubePage({searchParams}:{searchParams:Promise<{error?:string}>}){const {error}=await searchParams;return <main className="admin-sub"><div className="admin-top"><div><p>AUTHORIZED IMPORT</p><h1>YouTube Import</h1></div><Link className="secondary" href="/admin/videos">VIDEO LIBRARY</Link></div><form action={importYoutubeDraft} className="cms-form"><h2>Create YouTube draft</h2><p>TrapFeed stores the canonical YouTube ID and uses the official privacy-enhanced embed player. It never copies the video file. When a server-only API key is configured, title, description, and duration are filled from the official YouTube Data API; you can review them in the editor before publishing.</p>{error&&<div className="login-error">{error}</div>}<label>YouTube URL<input name="youtubeUrl" required placeholder="https://www.youtube.com/watch?v=..."/></label><label>Video title (optional with API key)<input name="title" maxLength={180}/></label><label>Description override<textarea name="description"/></label><button className="primary">IMPORT AS DRAFT</button></form></main>}
