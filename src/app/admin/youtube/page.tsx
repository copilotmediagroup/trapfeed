import Link from 'next/link';import {importYoutubeDraft} from './actions';
export default async function YoutubePage({searchParams}:{searchParams:Promise<{error?:string}>}){const {error}=await searchParams;return <main className="admin-sub"><div className="admin-top"><div><p>AUTHORIZED IMPORT</p><h1>YouTube Import</h1></div><Link className="secondary" href="/admin/videos">VIDEO LIBRARY</Link></div><form action={importYoutubeDraft} className="cms-form"><h2>Create YouTube draft</h2><p>TrapFeed stores the YouTube video ID and uses the official embed player. It does not copy or download the YouTube file.</p>{error&&<div className="login-error">{error}</div>}<label>Video title<input name="title" required/></label><label>YouTube URL<input name="youtubeUrl" required placeholder="https://www.youtube.com/watch?v=..."/></label><button className="primary">IMPORT AS DRAFT</button></form></main>}
