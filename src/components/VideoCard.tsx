import { Play } from 'lucide-react'; import type { Video } from '@/data/videos';
export function VideoCard({video,compact=false}:{video:Video;compact?:boolean}){return <article className={compact?'video-card compact':'video-card'}><div className={`thumb ${video.accent}`}><div className="thumb-noise"/><button className="play" aria-label={`Play ${video.title}`}><Play fill="currentColor"/></button><span className="duration">{video.duration}</span><span className="thumb-word">TRAPFEED</span></div><div className="video-copy"><span className="eyebrow">{video.category}</span><h3>{video.title}</h3><p>{video.views} views <b>•</b> {video.age}</p></div></article>}
