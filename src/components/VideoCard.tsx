import Link from 'next/link';import {Play} from 'lucide-react';
export type FeedVideo={id:string;title:string;slug:string;category:string;views:string;age:string;duration:string;thumbnailUrl?:string|null};
export function VideoCard({video,compact=false}:{video:FeedVideo;compact?:boolean}){return <article className={compact?'video-card compact':'video-card'}><Link href={`/watch/${video.slug}`} className="card-link"><div className="thumb" style={video.thumbnailUrl?{backgroundImage:`linear-gradient(0deg,rgba(0,0,0,.25),rgba(0,0,0,.05)),url(${video.thumbnailUrl})`}:undefined}><div className="thumb-noise"/><span className="play" aria-label={`Play ${video.title}`}><Play fill="currentColor"/></span>{video.duration&&<span className="duration">{video.duration}</span>}<span className="thumb-word">TRAPFEED</span></div><div className="video-copy"><span className="eyebrow">{video.category}</span><h3>{video.title}</h3><p>{video.views} views <b>•</b> {video.age}</p></div></Link></article>}
